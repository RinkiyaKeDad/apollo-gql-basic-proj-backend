# apollo-gql-basic-proj-backend
# Apollo GraphQL Basic Project

A basic project built using Apollo Server for GraphQL and using the GitHub API. The backend fetches users from the API and makes them available through GraphQL queries to the front end using GraphQL Resolvers.
